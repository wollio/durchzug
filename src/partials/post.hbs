<article class="{{post_class}} {{#if @rowStart}} md:col-span-2 {{/if}}">
  <div class="card-body">


    {{!--#if feature_image}}
    {{!-- This is a responsive image, it loads different sizes depending on device
  https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
    {{!--
    <img class="post-card-image object-cover"
           srcset="{{img_url feature_image size="s"}} 300w,
      {{img_url feature_image size="m"}} 600w,
      {{img_url feature_image size="l"}} 1000w,
      {{img_url feature_image size="xl"}} 2000w"
           sizes="100vw"
           src="{{img_url feature_image size="m"}}"
           alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
           loading="lazy"
      />
    {{/if --}}
    <time class="text-xs text-gray-400 font-sans" datetime="{{date format="YYYY-MM-DD"}}">{{date format="DD.MM.YYYY"}}</time>
    <a href="{{url}}">
      <h2 class="font-bold text-3xl">{{title}}</h2>
      <p class="">
        {{#if @rowStart}}
          {{excerpt characters="280"}}
        {{else}}
          {{excerpt characters="140"}}
        {{/if}}
      </p>
    </a>

  </div>{{!--/.post-card-content--}}


</article>
