<!DOCTYPE html>
<html lang="{{@site.locale}}">

<head>
    <title>{{meta_title}}</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=0">
    <meta name="theme-color" content="#000000">

    <script src="http://localhost:35729/livereload.js"></script>

    {{!-- Dynamic header content for SEO, meta tags, code injection etc --}}
    {{ghost_head}}
</head>

<body class="{{body_class}}">
    {{!-- Navigation bar --}}

    <div class="black-border p-6 pt-12 z-[9999]">
        <div></div>
    </div>

    <header class="top-0 w-full z-[10000] pt-3 flex justify-center text-white text-lg sm:text-base fixed flex-col items-center">
      <nav id="primaryNavigation" class="px-6 w-full lowercase transition-all grid grid-cols-3 gap-4 rounded-2xl flex-grow items-center {{^is "page"}}{{#if post.feature_image}}py-2 px-4{{/if}}{{/is}}">
        <div class="flex items-center col-span-2 sm:col-span-1 font-medium">
          <a href="{{@site.url}}">
            {{@site.title}}
          </a>
        </div>
        <button class="menu sm:col-span-2 sm:hidden justify-self-end align-self-center"></button>
        {{> "header"}}
      </nav>
    </header>
    {{!-- End Navbar --}}

    {{!-- All the main content gets inserted here, index.hbs, post.hbs, etc --}}
    {{{body}}}

    {{!-- Dynamic footer content from code injection etc --}}
    {{ghost_foot}}
</body>

</html>
