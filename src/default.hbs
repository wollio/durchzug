<!DOCTYPE html>
<html lang="{{@site.locale}}">

<head>
    <title>{{meta_title}}</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=0">

    <script src="http://localhost:35729/livereload.js"></script>

    {{!-- Dynamic header content for SEO, meta tags, code injection etc --}}
    {{ghost_head}}
</head>

<body class="{{body_class}}">
    {{!-- Navigation bar --}}

    <header class="w-full z-[10000] px-4 md:px-0 flex justify-center text-lg sm:text-base fixed top-6 flex-col items-center newNav">
      <nav id="primaryNavigation" class="hidden md:container transition-all bg-white grid grid-cols-3 gap-4 rounded-2xl flex-grow items-center {{^is "page"}}{{#if post.feature_image}}py-2 px-4{{/if}}{{/is}}">
        <div class="flex items-center col-span-2 sm:col-span-1 font-medium">
          <a href="{{@site.url}}">
            {{@site.title}}
          </a>
        </div>
        <button class="menu sm:col-span-2 sm:hidden justify-self-end align-self-center"></button>
        {{> "header"}}
      </nav>

      <nav class="newNavBar md:container transition-all grid grid-cols-3 gap-4 rounded-2xl flex-grow items-center">
        <div class="flex items-center col-span-2 sm:col-span-1 font-medium">
          <a href="{{@site.url}}">
            {{@site.title}}
          </a>
        </div>
        <button class="menu sm:col-span-2 sm:hidden justify-self-end align-self-center"></button>
        {{> "header"}}
      </nav>
    </header>
    {{!-- End Navbar --}}

    {{!-- All the main content gets inserted here, index.hbs, post.hbs, etc --}}
    {{{body}}}

    {{!-- Footer content goes here --}}
    <footer class="bg-gray-100 text-gray-900 pt-16 pb-8 text-md sm:text-xs">
        <div class="md:container mx-auto p-4 flex justify-between">
          <div class="inline">
            {{#if @site.logo}}
              <img src="{{@site.logo}}" class="inline mr-2" width="30px" height="30px" alt="Blog Logo" />
            {{/if}}
            <span class="">{{@site.title}}</span>
          </div>
          🌱
          <ul>
            <li><a class="leading-5" href="mailto:david@wollschlegel.com">E-Mail</a></li>
            <li><a class="leading-5" href="https://www.linkedin.com/in/david-wollschlegel/">LinkedIn</a></li>
            <li><a class="leading-5" href="https://www.instagram.com/wollio_/">Instagram</a></li>
            <li><a class="leading-5" href="https://twitter.com/wollio_">Twitter</a></li>
            <li>
              <a class="leading-5 text-green-500" href="#/portal">
                {{#if @member}}
                  My membership
                {{else}}
                  Newsletter
                {{/if}}
              </a>
            </li>
          </ul>
        </div>
    </footer>
    {{!-- End of footer --}}

    {{!-- Dynamic footer content from code injection etc --}}
    {{ghost_foot}}
</body>

</html>
